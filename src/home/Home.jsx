import { useState } from 'react';
import Nav from '../nav/Nav';
import { StyleSheet, Text, View } from 'react-native';

const Home = () => {
  return (
    <View>
      <Nav />
    </View>
  );
};

export default Home;
